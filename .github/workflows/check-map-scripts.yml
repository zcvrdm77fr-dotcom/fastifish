name: Check fishing map scripts

on:
  workflow_dispatch:
  push:
    paths:
      - 'fishing-structures.js'
      - 'depth-structures.js'
      - 'depth-wfs-utils.js'
      - 'gtk-substrate.js'
      - 'gtk-habitats.js'
      - 'velmu-fish.js'
      - 'index.html'
      - 'scripts/check-index-inline.mjs'
      - 'scripts/check-traficom-wfs.mjs'
      - 'sw.js'
      - 'vendor/leaflet/**'
      - '.github/workflows/check-map-scripts.yml'
  pull_request:
    paths:
      - 'fishing-structures.js'
      - 'depth-structures.js'
      - 'depth-wfs-utils.js'
      - 'gtk-substrate.js'
      - 'gtk-habitats.js'
      - 'velmu-fish.js'
      - 'index.html'
      - 'scripts/check-index-inline.mjs'
      - 'scripts/check-traficom-wfs.mjs'
      - 'sw.js'
      - 'vendor/leaflet/**'
      - '.github/workflows/check-map-scripts.yml'

permissions:
  contents: read

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check JavaScript syntax
        run: |
          node --check fishing-structures.js
          node --check depth-structures.js
          node --check depth-wfs-utils.js
          node --check gtk-substrate.js
          node --check gtk-habitats.js
          node --check velmu-fish.js
          node --check sw.js
          node --check scripts/check-traficom-wfs.mjs
      - name: Check inline JavaScript syntax
        run: node scripts/check-index-inline.mjs index.html
      - name: Smoke test Traficom WFS depth data
        run: node scripts/check-traficom-wfs.mjs
